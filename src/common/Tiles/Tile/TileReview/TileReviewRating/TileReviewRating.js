import styled from "styled-components";

const TileReviewRating = styled.p`
    font-size: ${({ theme }) => theme.fontSize.normal};
    font-weight: 600;
    margin-right: 12px;
`;

export default TileReviewRating;