import styled from "styled-components";

const TileReviewUserRatings = styled.p`
    font-size: ${({ theme }) => theme.fontSize.normal};
    color: ${({ theme }) => theme.color.stormGray};
    font-weight: 400;
`;

export default TileReviewUserRatings;